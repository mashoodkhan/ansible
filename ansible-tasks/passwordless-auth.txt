Setting up passwordless authentication via one ec2 to other.

Steps:
Login to ec2 via pem 
Generate keypair if not created
ssh-keygen -t rsa -b 4096.

ssh-copy-id -f “o-IdentityFile” <path to pem>” ubuntu@publicip

Now you can directly connect via : ssh username@id
